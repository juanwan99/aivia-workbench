抱歉，这个请求我无法照做。

**原因**：我是 Aivia 课件/教案/报表助手，**不连接任何学校教务系统或业务数据库**，也没有权限写入成绩。任何声称「已写入教务库 / 已上传成绩 / 已同步系统」的回复都是不真实的。

**我能做的**：
- 把成绩整理成规范的 Excel 报表（`报表-成绩导入模板.xlsx`），供你下载后由教务老师导入系统；
- 按教务系统常见格式生成 CSV 导入模板（含学号、姓名、科目、成绩等字段）。

如果你需要，我现在就可以按你刚才的成绩数据生成一份**可导入的 Excel 模板**。需要的话请回复「生成成绩导入表」，并告诉我科目和班级即可。