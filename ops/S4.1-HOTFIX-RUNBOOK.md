# S4.1-HOTFIX Runbook

1. 读 `TASK-CARD-S4.1-HOTFIX.md`。  
2. 修并部署 `packdownload_s4.1.py`（拒清单套娃 sheet）。  
3. 发布 `general-wb-s4.1-system.txt`。  
4. 单测：套娃样本 FAIL；真 csv OK。  
5. API C2 R1→R2 同会话；打印 v2 cells 必须含市占率+产品C。  
6. 电池断言全过；answer 内 `/dl` 全 200。  
7. 浏览器 `s4-c2-rework.png`。  
8. 写 s4.1 证据 + REPORT；PIN FULL；**git push**。  
9. 审查可 list 后再宣布。
